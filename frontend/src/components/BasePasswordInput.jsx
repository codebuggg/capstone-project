import BaseInput from "./BaseInput";

const BasePasswordInput = (props) => {
    return (
        <BaseInput
            type="password"
            {...props}
        />
    )
}

export default BasePasswordInput;